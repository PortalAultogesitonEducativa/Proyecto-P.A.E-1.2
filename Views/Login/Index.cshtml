@{
    ViewData["Title"] = "Iniciar Sesión";
}

<style>
    /* Ambos botones comparten el mismo color azul base */
    .btn-login-main {
        background-color: #0d2a4a;
        border-radius: 10px;
        border: none;
        transition: all 0.3s ease;
    }
        /* Al pasar el mouse: se aclara un poco */
        .btn-login-main:hover {
            background-color: #143d6a;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(13, 42, 74, 0.25);
        }

    /* Botón de Olvidé mi contraseña con el mismo color azul base */
    .btn-login-forgot {
        background-color: #0d2a4a;
        color: #ffffff !important;
        border-radius: 10px;
        border: none;
        transition: all 0.3s ease;
    }
        /* Al pasar el mouse: se oscurece un poco para diferenciarse */
        .btn-login-forgot:hover {
            background-color: #081a2e;
            transform: translateY(-2px);
            box-shadow: 0 4px 8px rgba(13, 42, 74, 0.25);
        }
</style>

<div class="container my-5 py-5">
    <div class="row justify-content-center">
        <div class="col-md-5">
            <div class="card border-0 shadow-lg" style="border-radius: 20px; background-color: #fdf8f3;">
                <div class="card-body p-5">
                    <div class="text-center mb-4">
                        <img src="~/images/logo.png" alt="PAE" style="height: 80px;" class="mb-3">
                        <h3 class="fw-bold" style="color: #0d2a4a;">Bienvenido a PAE</h3>
                        <p class="text-muted small">Portal de Autogestión Educativa</p>
                    </div>

                    <form asp-action="Ingresar" asp-controller="Login" method="post">
                        <div class="mb-3">
                            <label class="form-label fw-bold">Usuario o Correo</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="bi bi-person"></i></span>
                                <input type="text" name="correo" class="form-control" placeholder="correo@ejemplo.com" required>
                            </div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">Contraseña</label>
                            <div class="input-group">
                                <span class="input-group-text bg-light border-end-0"><i class="bi bi-lock"></i></span>
                                <input type="password" id="passwordInput" name="password" class="form-control bg-light border-start-0 border-end-0" placeholder="••••••••" required>
                                <button class="btn btn-light border border-start-0" type="button" onclick="togglePassword()">
                                    <i id="toggleIcon" class="bi bi-eye"></i>
                                </button>
                            </div>
                        </div>

                        <div class="d-grid gap-3 mb-3">
                            <button type="submit" class="btn btn-lg text-white btn-login-main">
                                Ingresar al Sistema
                            </button>

                            <a href="/Login/OlvideContraseña" class="btn btn-lg text-white btn-login-forgot d-flex justify-content-center align-items-center">
                                <i class="bi bi-question-circle me-2"></i> ¿Olvidó su contraseña?
                            </a>
                        </div>

                        <p class="text-danger">@ViewBag.Error</p>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
    function togglePassword() {
        const passwordInput = document.getElementById('passwordInput');
        const toggleIcon = document.getElementById('toggleIcon');

        if (passwordInput.type === 'password') {
            passwordInput.type = 'text';
            toggleIcon.classList.replace('bi-eye', 'bi-eye-slash');
        } else {
            passwordInput.type = 'password';
            toggleIcon.classList.replace('bi-eye-slash', 'bi-eye');
        }
    }
</script>